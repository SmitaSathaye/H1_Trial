{
  "name": "saucedemo-homework",
  "version": "2.2.0",
  "description": "This project is created to complete the work assignment.",
  "main": "index.js",
  "scripts": {
    "open": "npx cypress open --e2e --browser chrome",
    "chrome": "npx cypress run --e2e --browser chrome",
    "edge": "npx cypress run --e2e --browser edge",
    "runTHRNDL-536": "npx cypress run --e2e --browser chrome --env tags=\"@THRNDL-536\"",
    "runPositive": "npx cypress run --e2e --browser chrome --env tags=\"@Positve\"",
    "runNegative": "npx cypress run --e2e --browser chrome --env tags=\"@Negative\"",
    "runPurchase": "npx cypress run --e2e --browser chrome --env tags=\"@Purchase\"",
    "runLogin": "npx cypress run --e2e --browser chrome --env tags=\"@Login\""
  },
  "keywords": [
    "cypress"
  ],
  "author": "Smita",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.0.0",
    "@bahmutov/cy-api": "^2.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "@faker-js/faker": "^7.6.0",  
    "cypress": "^12.0.2",
    "cypress-mochawesome-reporter": "^3.2.3",
    "esbuild": "^0.16.4"
  }
}
